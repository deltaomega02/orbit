# OrbitApp 프로젝트 아키텍처

이 문서는 AI 스타일리스트 및 가상 착용 시뮬레이터인 OrbitApp의 프로젝트 구조에 대한 자세한 개요를 제공합니다.

## 루트 디렉토리

루트 디렉토리에는 프로젝트의 주요 구성 파일이 포함되어 있습니다.

- **`.gitignore`**: Git에서 의도적으로 추적하지 않는 파일 및 폴더를 나열합니다. 여기에는 `node_modules`, `.expo` 및 기타 개발 관련 파일이 포함됩니다.
- **`app.json`**: Expo의 구성 파일입니다. 이름, 버전, 아이콘, 스플래시 화면과 같은 필수 앱 속성과 iOS 및 Android에 대한 플랫폼별 설정을 정의합니다. 또한 카메라 및 사진 라이브러리 접근에 필요한 권한을 지정합니다.
- **`App.tsx`**: 애플리케이션의 메인 진입점입니다. Redux 스토어를 초기화하고, 내비게이션 컨테이너를 설정하며, 기기별 안전 영역을 처리하기 위해 앱을 `SafeAreaProvider`로 래핑합니다.
- **`babel.config.js`**: JavaScript 컴파일러인 Babel의 구성 파일입니다. Expo용 프리셋과 NativeWind(Tailwind CSS용) 및 Reanimated(애니메이션용) 플러그인이 포함되어 있습니다.
- **`eas.json`**: Expo Application Services (EAS)의 구성 파일입니다. 이 파일은 앱을 빌드하고 앱 스토어에 제출하는 데 사용됩니다.
- **`package.json`**: 프로젝트의 메타데이터, 종속성 및 스크립트를 정의합니다. Expo, React Navigation, Redux, Axios와 같은 프로젝트에서 사용되는 모든 라이브러리를 나열합니다.
- **`README.md`**: 프로젝트에 대한 간략한 소개 문서입니다.
- **`tsconfig.json`**: TypeScript의 구성 파일입니다. 더 깔끔하고 유지보수하기 쉬운 임포트를 위한 경로 별칭을 포함하여 컴파일러 옵션을 설정합니다.

## `src` 디렉토리

`src` 디렉토리에는 다음 하위 디렉토리로 구성된 애플리케이션의 핵심 소스 코드가 포함되어 있습니다.

### `api`

- **`config.ts`**: 이 파일은 백엔드 서버의 기본 URL과 같은 API 클라이언트에 대한 구성을 포함할 가능성이 높습니다.

### `assets`

이 디렉토리에는 애플리케이션의 모든 정적 자산이 포함되어 있습니다.

- **`images`**: 앱 아이콘, 스플래시 화면 및 기타 시각적 자산과 같은 이미지 파일이 포함되어 있습니다.

### `constants`

이 디렉토리에는 애플리케이션 전체에서 사용되는 상수 값이 포함되어 있습니다.

- **`colors.ts`**: 앱에서 사용되는 색상 팔레트를 정의합니다.
- **`config.ts`**: 앱의 전역 구성 설정을 포함합니다.
- **`dimensions.ts`**: 표준 치수 및 간격 값을 정의합니다.
- **`typography.ts`**: 글꼴 크기 및 두께와 같은 타이포그래피 스타일을 정의합니다.

### `navigation`

이 디렉토리에는 애플리케이션의 내비게이션 로직이 포함되어 있습니다.

- **`AppNavigator.tsx`**: 앱의 메인 내비게이터입니다. 다른 화면 및 내비게이터 간 전환 로직을 포함할 가능성이 높습니다.
- **`TabNavigator.tsx`**: 화면 하단에 표시되는 탭 내비게이터입니다.
- **`types.ts`**: 내비게이션 속성에 대한 TypeScript 타입 정의를 포함합니다.

### `screens`

이 디렉토리에는 기능별로 구성된 애플리케이션의 모든 화면이 포함되어 있습니다.

- **`Auth`**: 로그인 및 등록과 같은 사용자 인증 관련 화면을 포함합니다.
- **`Camera`**: 의류 품목 사진 촬영을 위한 카메라 화면을 포함합니다.
- **`Closet`**: 사용자의 가상 옷장을 포함하며, 의류 품목을 표시합니다.
- **`Home`**: 앱의 메인 화면으로, 의상 추천 또는 기타 개인화된 콘텐츠를 표시할 수 있습니다.
- **`Onboarding`**: 신규 사용자에게 표시되는 온보딩 화면을 포함합니다.
- **`Outfit`**: 의상 생성 및 보기 관련 화면을 포함합니다.
- **`Settings`**: 사용자 설정 화면을 포함합니다.

### `services`

이 디렉토리에는 외부 API와 상호 작용하는 서비스가 포함되어 있습니다.

- **`auth`**: 로그인 및 회원가입과 같은 사용자 인증 관련 서비스를 포함합니다.

### `store`

이 디렉토리에는 애플리케이션의 상태 관리를 위한 Redux 스토어가 포함되어 있습니다.

- **`index.ts`**: Redux 스토어의 진입점입니다.
- **`slices`**: 앱 상태의 특정 부분을 관리하는 Redux 슬라이스를 포함합니다.
    - **`closetSlice.ts`**: 사용자 옷장의 상태를 관리합니다.
    - **`outfitSlice.ts`**: 사용자 의상의 상태를 관리합니다.
    - **`uiSlice.ts`**: 로딩 표시기 및 모달과 같은 UI 상태를 관리합니다.
    - **`userSlice.ts`**: 현재 사용자의 상태를 관리합니다.

### `types`

이 디렉토리에는 애플리케이션에서 사용되는 데이터 구조에 대한 TypeScript 타입 정의가 포함되어 있습니다.

- **`clothes.ts`**: 의류 품목에 대한 타입을 정의합니다.
- **`outfit.ts`**: 의상에 대한 타입을 정의합니다.
- **`user.ts`**: 사용자에 대한 타입을 정의합니다.